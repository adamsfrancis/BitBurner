export async function main(ns) {
    const target = ns.args[0];
    const start = parseInt(ns.args[1]);
    const wait = start - (new Date(Date.now()+ns.args[2]));
    await ns.sleep(wait);
    await ns.weaken(target);
}